const tabs = [['waves', 'Waves'], ['wind', 'Wind'], ['temperature', 'Temperature']]
function points(values) { const min = Math.min(...values); const max = Math.max(...values); const range = max - min || 1; return values.map((value, index) => `${(index / (values.length - 1)) * 100},${84 - ((value - min) / range) * 58}`).join(' ') }

export default function ConditionsChart({ trends, activeTab, onTabChange }) {
  const trend = trends[activeTab]
  return <section className="conditions-chart panel"><div className="panel-title"><div><p className="eyebrow">CONDITIONS TREND</p><h2>Last 12 hours</h2></div><strong>{trend.current}</strong></div><div className="chart-tabs" role="tablist" aria-label="Condition trend type">{tabs.map(([id, label]) => <button key={id} role="tab" aria-selected={activeTab === id} className={activeTab === id ? 'active' : ''} onClick={() => onTabChange(id)}>{label}</button>)}</div><div className="chart-wrap"><svg viewBox="0 0 100 100" preserveAspectRatio="none" role="img" aria-label={`${trend.label} trend`}><defs><linearGradient id="chart-fill" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="#1daed2" stopOpacity=".32" /><stop offset="100%" stopColor="#1daed2" stopOpacity="0" /></linearGradient></defs><path className="chart-area" d={`M 0,100 L ${points(trend.values)} L 100,100 Z`} /><polyline className="chart-line" points={points(trend.values)} /></svg><span className="chart-label first">12h ago</span><span className="chart-label middle">6h ago</span><span className="chart-label last">Now</span></div><p className="chart-caption">{trend.label} · {trend.direction || 'Stable'} · Sample values for the selected location</p></section>
}
