import { recentActivity } from '../../data/dashboardData'
export default function RecentActivity({ onSelect }) { return <section className="panel activity"><div className="panel-title"><div><p className="eyebrow">QUERY HISTORY</p><h2>Recent activity</h2></div><button aria-label="View all recent activity">View all <span aria-hidden="true">-&gt;</span></button></div>{recentActivity.map((item) => <button className="activity-row" key={item.title} onClick={() => onSelect(item)}><span>{item.icon}</span><div><b>{item.title}</b><small>{item.category} · {item.time}</small></div><i aria-hidden="true">-&gt;</i></button>)}</section> }
